求解，该怎样解答，求解答求解释
#include "stdio.h"
main()
{
    float ar[10],*p;
    void inc(float *pt,int n); 
    for(p=ar;p<ar+10;p++)
        scanf("___输入各个元素值%f ____",p);
    inc(____ar_____,____p______);
    for(p=ar;p<ar+10;p++)
        printf("%-8.2f ",*p);
}
void inc(float *pt,int n)
{
    float *p1=pt;
    int m=0;
    for(;_m<10___;m++,p1++)
        *p1=*p1+10;
}希望看看错在哪，帮忙讲一下 
----------------------------------------------------------------------------------------------------
1.scanf里面不要加字符串,否则必须和字符串输入一样才能读到值
2.你的inc函数好像第二个参数根本没用?
<code> #include "stdio.h"
void main()
{
    float ar[10],*p;
    void inc(float *pt,int n); 
    printf("输入各个元素值:");
    for(p=ar;p<ar+10;p++)
        scanf("%f",p);
    inc(ar,p-ar);
    for(p=ar;p<ar+10;p++)
        printf("%-8.2f ",*p);
}
void inc(float *pt,int n)
{
    float *p1=pt;
    int m=0;
    for(;m<10;m++,p1++)
        *p1=*p1+10;
}

</code>
